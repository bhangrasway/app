Made a simple dance students management software for personal use.

Run it with:

```powershell
python server.py
```

Open:

```text
http://127.0.0.1:8002
```

Login credentials:

- Username: `admin`
- Password: `jindjaan`

The credentials are now checked on the local Python server instead of in the browser page source.
